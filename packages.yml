# ============================================
# packages.yml
# 외부 dbt 패키지 의존성 선언. 'dbt deps'로 설치됨.
# 설치되면 dbt_packages/ 폴더에 받아짐 (.gitignore 처리 — 각자 dbt deps로 재현)
# ============================================

packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
